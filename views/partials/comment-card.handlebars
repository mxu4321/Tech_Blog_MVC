<div class="comment-card list-group">
    {{!-- ⏰ 以下div原本是 a href="#" --}}
<div href="#" class="list-group-item list-group-item-action">
    <div class="d-flex w-200 justify-content-between">
        <h5 class="mb-1">{{comment_text}}</h5>
        <small class="text-muted">
            on
            {{!-- ⏰ insert comment date & time --}}
            {{format_date}}
        </small>
    </div>
    <p class="mb-1">
        by
        {{user.username}}
    </p>
</div>
</div>



